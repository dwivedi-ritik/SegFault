---
title: Classic button styling in css
---
import BtnComponent from '../../components/btn.js'

## Classic button styling in css

<BtnComponent />

### Tempate code for this style.

HTML code

```html
<a class="cta-link" href="/constructor">Go Create!</a>
```

CSS Code

```css
a{
  color:black;
  font-family:Barlow;
}

.cta-link {
    border: 4px solid #000;
    display: inline-block;
    vertical-align: middle;
    background-color: #f456ab;
    padding: 18px 53px;
    font-weight: 500;
    font-size: 28px;
    line-height: 1.15;
    text-decoration:none;
    position: relative;
}

.cta-link::before , .cta-link::after{
    border: 4px solid #000;
    content: '';
    display: block;
    position: absolute;
    z-index: 1;
}

.cta-link:before{
    border-bottom: none;
    border-left: none;
    bottom: -10px;
    right: -16px;
    width: 12px;
    height: calc(100% + 8px);
    -webkit-transform: skewY(45deg);
    transform: skewY(45deg);
}


.cta-link:after{
    border-top: none;
    border-right: none;
    bottom: -16px;
    right: -10px;
    width: calc(100% + 8px);
    height: 12px;
    transform: skewX(45deg);
}

* , :before  ,:after{
  box-sizing:border-box;
}

```